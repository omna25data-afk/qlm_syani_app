name: qlm_syani_app
description: "A new Flutter project for QLM Syani System."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3

  # Database (Isar)
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1 # Includes binaries for Android/iOS
  path_provider: ^2.1.2

  # Networking
  dio: ^5.4.0
  dio_cache_interceptor: ^3.5.0
  
  # Utility
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  flutter_secure_storage: ^9.0.0
  connectivity_plus: ^5.0.2
  uuid: ^4.2.2
  intl: ^0.19.0
  
  # UI/UX
  flutter_screenutil: ^5.9.0
  go_router: ^12.1.0
  flutter_animate: ^4.5.0
  fl_chart: ^0.66.0
  google_fonts: ^6.1.0
  heroicons: ^0.8.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
  # Code Generation
  build_runner: ^2.4.8
  riverpod_generator: ^2.3.9
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  isar_generator: ^3.1.0+1

flutter:
  uses-material-design: true
